# Node modules
node_modules

# Build / framework outputs
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.code-workspace
.AppleDouble
.LSOverride

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Caches / tooling artifacts
.cache
.vite
.eslintcache
*.tsbuildinfo
coverage
.nyc_output

# Env files (examples remain tracked)
.env
.env.*
!.env.example
!.env.test

# Local secrets folder (do not commit)
/secrets/
/.secrets/
/env/
!env/README.md

# Vite timestamp artifacts
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Cypress artifacts
cypress/screenshots
cypress/videos

# Prisma local dev databases (optional)
prisma/*.db
prisma/*.sqlite
prisma/*.db-journal
prisma/*.sqlite-journal

# Remove invalid/unused ignores
# (was "/../generated/prisma" which is ineffective and removed)
